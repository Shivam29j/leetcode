package com.company;

public class recursivefabnoic {
    public  static void main(String[] args) {
        int n;
        int ans =fibo( (n=7));
        System.out.println(ans);

    }
    static int fibo(int n) {
        if (n < 2) {
            return n;
        }
        return fibo(n = n - 1) + fibo(n = n - 2);
    }
}
